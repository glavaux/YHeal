Before build:
Check that the HEALPIX_BUILD environment variable in Makefile is
set to the correct value for your healpix environment.

To build, run the commands:

yorick -batch make.i
make

To install:
make install
